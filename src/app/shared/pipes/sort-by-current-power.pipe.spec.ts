import { SortByCurrentPowerPipe } from './sort-by-current-power.pipe';

describe('SortByCurrentPowerPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByCurrentPowerPipe();
    expect(pipe).toBeTruthy();
  });
});
